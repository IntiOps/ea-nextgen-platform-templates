# Knowledge assistant with retrieval

Design retrieval, model interaction and evaluation before connecting a model.

Import as a proposed model, review identities and unresolved decisions, then capture an architecture version. No cloud credentials are needed. This blueprint does not deploy infrastructure.

## Coverage gaps

- Corpus retrieval and evaluation relationships are not yet defined in the platform metamodel.
- Do not infer that dataset training is equivalent to retrieval.
