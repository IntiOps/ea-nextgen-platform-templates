# Agent with read-only tools

Associate a business capability with a model and explicitly scoped tools.

Import as a proposed model, review identities and unresolved decisions, then capture an architecture version. No cloud credentials are needed. This blueprint does not deploy infrastructure.

## Coverage gaps

- Read-only tool permissions require a runtime policy and enforcement; graph edges do not grant access.
