# Queue worker implementation binding

Design-only binding to the local reference component. No deployable variant is advertised by catalog/index.yaml. Complete production adapters, infrastructure, native CI, costs and cleanup before promotion.
